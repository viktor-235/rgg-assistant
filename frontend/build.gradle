plugins {
    id 'org.siouan.frontend-jdk11' version '6.0.0' // https://siouan.github.io/frontend-gradle-plugin/
}

frontend {
    nodeVersion = '18.12.1'
    assembleScript = 'run build'
    cleanScript = 'run clean'
    checkScript = 'run test'
}
